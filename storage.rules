rules_version = '2';

// MVP/SCAFFOLD — open upload path for local dev; tighten before production.
service firebase.storage {
  match /b/{bucket}/o {
    match /resumes/uploads/{allPaths=**} {
      allow read, write: if true;
    }
  }
}
